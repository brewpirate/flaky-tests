{
  "name": "flaky-tests",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "bun x --bun @biomejs/biome format",
    "format:fix": "bun x @biomejs/biome format --write",
    "lint": "bun x @biomejs/biome lint",
    "lint:fix": "bun x @biomejs/biome lint --fix",
    "test": "bun test",
    "typecheck": "for p in packages/core packages/cli packages/store-sqlite packages/store-postgres packages/store-supabase packages/store-turso packages/plugin-bun packages/plugin-vitest; do echo \"== $p ==\" && tsc -p $p --noEmit || exit 1; done",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^2.1.6",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@biomejs/biome": "2.4.12"
  }
}