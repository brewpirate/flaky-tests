pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: '*.{ts,tsx,js,jsx,mjs,cjs,css,json,jsonc}'
      run: bun x @biomejs/biome check --no-errors-on-unmatched --staged --write --files-ignore-unknown=true {staged_files}
      stage_fixed: true
